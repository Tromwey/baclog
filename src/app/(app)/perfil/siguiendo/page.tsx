import { PeopleScreen } from "../people-screen";

/** F3.10 — a quién sigues (owner-only; see people-screen.tsx). */
export default function SiguiendoPage() {
  return <PeopleScreen mode="following" />;
}
