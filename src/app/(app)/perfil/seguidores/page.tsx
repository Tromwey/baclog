import { PeopleScreen } from "../people-screen";

/** F3.10 — quién te sigue (owner-only; see people-screen.tsx). */
export default function SeguidoresPage() {
  return <PeopleScreen mode="followers" />;
}
